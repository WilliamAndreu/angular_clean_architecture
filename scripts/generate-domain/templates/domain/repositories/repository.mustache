import { Observable } from 'rxjs';
import { {{className}}Entity } from '@models/{{name}}/{{name}}-entity.model';

export abstract class {{className}}Repository {
  abstract get{{className}}(params: unknown): Observable<{{className}}Entity>;
}
