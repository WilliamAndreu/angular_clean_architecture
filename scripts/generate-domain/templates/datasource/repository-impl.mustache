import { inject, Injectable } from '@angular/core';
import { Observable, switchMap, tap, of } from 'rxjs';
import { map } from 'rxjs/operators';
import { {{className}}Repository } from '@repositories/{{name}}/{{name}}.repository';
import { {{className}}Entity } from '@models/{{name}}/{{name}}-entity.model';
import { {{className}}RemoteDataSource } from '@data/datasource/{{name}}/source/{{name}}-remote.datasource';
import { {{className}}LocalDataSource } from '@data/datasource/{{name}}/source/{{name}}-local.datasource';
import { {{className}}DtoToEntityMapper } from './mappers/{{name}}-dto-to-entity.mapper';
import { {{className}}DboToEntityMapper } from './mappers/{{name}}-dbo-to-entity.mapper';

@Injectable()
export class {{className}}ImpRepository extends {{className}}Repository {
  private readonly remoteDataSource = inject({{className}}RemoteDataSource);
  private readonly localDataSource = inject({{className}}LocalDataSource);

  private readonly dtoMapper = new {{className}}DtoToEntityMapper();
  private readonly dboMapper = new {{className}}DboToEntityMapper();

  override get{{className}}(params: unknown): Observable<{{className}}Entity> {
    return this.localDataSource.get{{className}}(params).pipe(
      switchMap((cached) => {
        if (cached !== null) {
          return of(this.dboMapper.mapFrom(cached));
        }
        return this.remoteDataSource.get{{className}}(params).pipe(
          tap((dto) => this.localDataSource.save{{className}}(params, dto)),
          map((dto) => this.dtoMapper.mapFrom(dto)),
        );
      }),
    );
  }
}
